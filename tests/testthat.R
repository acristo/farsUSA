library(testthat)
library(farsUSA)

test_check("farsUSA")

test_that("ReadData", {
#  year <- 2011
#  t.filename <- make_filename(year)
#  expect_that(typeof(t.filename), matches("character"))
#  df.data <- fars_read(t.filename)
#  expect_that(fit, is_a("data.frame"))

})
